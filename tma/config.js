// Runtime configuration — edit before deploying
// For local testing: ws://localhost:8787
// For production: wss://nanobots-relay.YOUR_SUBDOMAIN.workers.dev
window.NANOBOTS_CONFIG = {
    relayUrl: "ws://localhost:8787",
    relayToken: "test-token-for-local-dev",
};
